

const IconButton = ({ children }) => {
    return (
        <div className="border rounded-md m-1 h-8 w-8 items-center flex justify-center">
            {children}
        </div>
    );
};

export default IconButton;